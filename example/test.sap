# file header comment

module test;

// test pragma
pragma option;

// test import
import common;

include "include/base.sap" # comment at end of include

attribute default { any value = 0; } /* default attribute */

include "include/base.sap" # duplicate include is safe

[name("Test")]
type test {
    [default]
    i32 x = 0 # a comment
    ;
    string y = "y"; // C++-style comment
    [ignore, name("fail")]
    /*
        C-style block comment
    */
    bool2 skipme = false;
    u32* iptr = null;
    string[] array;
    bool*[] bit_pointers;
}

type derived : test;

enum order {
    first, second, third
}

type ordered {
    order position = second;
}
