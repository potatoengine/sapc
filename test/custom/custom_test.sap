module custom_test;

import custom_base;

use local : struct;
use constexpr : const;
use typelist : union;
use newtype : using;

local LocalTest {
    int localX;
}

custom CustomTest {
    int customX;
}

flags FlagsTest { Red = 1, Green = 2, Blue = 4 }

constexpr int seven = 7;

newtype Int = int;

typelist Types { LocalTest l; CustomTest c; }
